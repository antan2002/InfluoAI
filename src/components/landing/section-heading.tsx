// TODO: Render section title + subtitle with consistent typography and optional badge

interface SectionHeadingProps {
  title: string;
  subtitle?: string;
  badge?: string;
}

export default function SectionHeading({
  title,
  subtitle,
  badge,
}: SectionHeadingProps) {
  return (
    <div>
      {/* TODO: Add optional badge/chip, title heading, subtitle paragraph */}
    </div>
  );
}
