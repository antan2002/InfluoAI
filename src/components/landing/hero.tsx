// TODO: Build hero section with headline, subheadline, CTA buttons, and background visuals

import { SITE_CONFIG } from "@/lib/constants/landing";

export default function Hero() {
  return (
    <section>
      {/* TODO: Add main heading, supporting text, primary/secondary CTA buttons, preview image */}
    </section>
  );
}
