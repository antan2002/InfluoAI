// TODO: Build responsive navigation bar with logo, nav links, CTA buttons, and mobile menu

import { NAV_LINKS, SITE_CONFIG } from "@/lib/constants/landing";

export default function Navbar() {
  return (
    <nav>
      {/* TODO: Add logo/brand, desktop nav links, auth buttons, mobile hamburger menu */}
    </nav>
  );
}
